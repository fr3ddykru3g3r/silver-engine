name: IRIS-SEP freshness crossover source readiness

on:
  push:
    branches:
      - codex/iris-sep-continuation-20260905
    paths:
      - 'iris_report/iris_sep/config/freshness_crossover_study_v1_preregistration_2026-09-09.json'
      - 'iris_report/iris_sep/config/freshness_crossover_source_probe_v1_2026-09-09.json'
      - 'iris_report/iris_sep/tools/run_freshness_crossover_source_probe.py'
      - 'iris_report/iris_sep/tests/test_freshness_crossover_source_probe.py'
      - '.github/workflows/iris-sep-freshness-crossover-source-readiness.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  source-readiness:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out frozen branch
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13.5'

      - name: Install bounded probe dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install requests==2.32.5 netCDF4==1.7.2 pytest==8.4.2

      - name: Compile and run offline source-contract tests
        run: |
          python -m py_compile iris_report/iris_sep/tools/run_freshness_crossover_source_probe.py
          python -m pytest -q iris_report/iris_sep/tests/test_freshness_crossover_source_probe.py

      - name: Run frozen freshness source-readiness probe
        run: |
          rm -rf /tmp/freshness-crossover-source-readiness
          python -m iris_report.iris_sep.tools.run_freshness_crossover_source_probe \
            --output /tmp/freshness-crossover-source-readiness

      - name: Bind runtime and commit
        if: always()
        run: |
          mkdir -p /tmp/freshness-crossover-source-readiness
          python - <<'PY'
          import json, platform
          from pathlib import Path
          import netCDF4, requests
          path = Path('/tmp/freshness-crossover-source-readiness/runtime.json')
          path.write_text(json.dumps({
            'git_head': '${{ github.sha }}',
            'python': platform.python_version(),
            'requests': requests.__version__,
            'netCDF4': netCDF4.__version__,
            'training_performed': False,
            'labels_derived': False,
            'forecast_skill_computed': False,
            'locked_test_accessed': False,
            'independent_final_evidence': False,
          }, indent=2, sort_keys=True) + '\n')
          PY

      - name: Upload immutable source-readiness evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: iris-sep-freshness-source-readiness-${{ github.sha }}
          path: /tmp/freshness-crossover-source-readiness
          if-no-files-found: warn
          retention-days: 30
